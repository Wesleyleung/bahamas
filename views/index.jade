extends layout

block head
    link(rel='stylesheet', href='/stylesheets/index_style.css')


block main

    .content-padded
        p Please enter your fixit request below

        div#upload_div(style="width:500px;height:100px;background-color:#C0C0C0;cursor:pointer", onclick="divClickFunction()")
            img(src="/img/camera_add.png")
            "tap to upload a picture"

        input#upload_input(type="file", style="visibility:hidden")

        //
            form(method="post", action="/request")
                label(for="photo")
                    | Take a picture to help us solve the problem!
                #photo-box
                    input#photo-input(type="file", name="photo", hidden="true")
                    .block-button#photo-button
                        | Click to take a Picture
            br
            input(type="file", name="photo")

        input(type="text", placeholder="Location")
        textarea(rows="5", placeholder="Descripition")
